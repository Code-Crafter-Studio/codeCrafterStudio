---
// Asumiendo que tienes las propiedades pasadas correctamente
interface Props {
  urlVideo: string;
}
const { urlVideo } = Astro.props;
---

<div
  class="flex justify-center items-center w-[300px] h-[600px] hover:scale-10"
>
  <div
    class="relative z-10 w-[284px] max-w-[300px] h-full max-h-[600px] overflow-visible"
  >
    <div class="absolute opacity-100">
      <video
        src={urlVideo}
        loop
        playsinline
        autoplay
        muted
        class="cursor-auto w-full h-full rounded-[35px] object-cover bg-transparent p-6"
      ></video>
    </div>
    <div class="absolute z-20">
      <img
        decoding="async"
        sizes="304px"
        src="/media/phone.webp"
        alt="Phone Frame"
        class="block w-full h-full rounded-inherit object-cover"
      />
    </div>
  </div>
</div>
